export enum ELStorage {
  accessToken = "accessToken",
}

export enum EBaseUrl {
  API_KEY = 'https://api.gevskitchen.com/api',
}


export enum ERequestStatus {
  FULFILLED = "fulfilled",
  PENDING = "pending",
  REJECTED = "rejected",
}
